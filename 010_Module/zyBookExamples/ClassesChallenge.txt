/* 10.5.1: Classes. What is the program output */


#include <iostream>
using namespace std;

class Person {
   public:
      void SetName(string nameToSet);
      string GetName();
   private:
      string name;
};

void Person::SetName(string nameToSet) {
   name = nameToSet;
}

string Person::GetName() {
   return name;
}

int main() {
   string userName;
   Person person1;

   userName = "Joe";

   person1.SetName(userName);
   cout << "You are " << person1.GetName();

   return 0;
}
// You are Joe 


#include <iostream>
using namespace std;

class Person {
   public:
      void SetName(string nameToSet);
      string GetName();
   private:
      string name;
};

void Person::SetName(string nameToSet) {
   name = nameToSet;
}

string Person::GetName() {
   return name;
}

int main() {
   string userName;
   Person person1;

   userName = "Ron";

   person1.SetName(userName);
   cout << "He is " << person1.GetName();

   return 0;
}
// He is Ron 



#include <iostream>
using namespace std;

class Person {
   public:
      void SetFirstName(string firstNameToSet);
      void SetLastName(string lastNameToSet);
      string GetFullName();
   private:
      string first;
      string last;
};

void Person::SetFirstName(string firstNameToSet) {
   first = firstNameToSet;
}

void Person::SetLastName(string lastNameToSet) {
   last = lastNameToSet;
}

string Person::GetFullName() {
   return  first + "_" + last;
}

int main() {
   string userFirstName;
   string userLastName;
   Person person1;

   userFirstName = "Max";
   userLastName = "Kent";

   person1.SetFirstName(userFirstName);
   person1.SetLastName(userLastName);
   cout << "He is " << person1.GetFullName();

   return 0;
}
// He is Max_Kent



#include <iostream>
using namespace std;

class Person {
   public:
      void SetName(string nameToSet);
      string GetName();
   private:
      string name;
};

void Person::SetName(string nameToSet) {
   name = nameToSet;
}

string Person::GetName() {
   return name;
}

int main() {
   string userName;
   string differentUserName;
   Person person1;

   userName = "Bob";
   differentUserName = "Tom";

   person1.SetName(userName);
   cout << person1.GetName();
   person1.SetName(differentUserName);
   cout << " is " << person1.GetName();

   return 0;
}
// Bob is Tom 



#include <iostream>
using namespace std;

class Person {
   public:
      void SetFirstName(string firstNameToSet);
      void SetLastName(string lastNameToSet);
      string GetFullName();
   private:
      string first;
      string last;
};

void Person::SetFirstName(string firstNameToSet) {
   first = firstNameToSet;
}

void Person::SetLastName(string lastNameToSet) {
   last = lastNameToSet;
}

string Person::GetFullName() {
   return last + " " + first;
}

int main() {
   string firstName;
   string lastName;

   firstName = "Ann";
   lastName = "Kent";

   Person person1;

   person1.SetFirstName(firstName);
   person1.SetLastName(lastName);

   cout << person1.GetFullName();

   return 0;
}
// 